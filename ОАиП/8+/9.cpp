#include "stdafx.h"

int maxSize2, curSize2 = 0;
int maxSize, curSize = 0, curSize1 = 0, curSize22 = 0;

int main() {
    setlocale(LC_ALL, "rus");
    bool work = true;
    while (work) {
        var9();
        break;
    }
    return 0;
}